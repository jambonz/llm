/**
 * Contract test-kit for @jambonz/llm adapters.
 *
 * This module exports a parameterized test suite that runs against every
 * adapter to verify it honors the contract. Any new vendor adapter PR must
 * pass this suite unchanged.
 *
 * The 20 checks are documented in the library plan; implementation lands in
 * step 2 of the sequencing plan.
 */

// Placeholder — populated in step 2.
export const CONTRACT_CHECKS_PLANNED = 20;
